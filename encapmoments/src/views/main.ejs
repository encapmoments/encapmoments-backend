<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>메인 페이지</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 100px; }
        .container { width: 300px; margin: auto; padding: 20px; }

        .btn {
            font-size: 16px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin: 10px 5px;
        }

        .logout-btn {
            background-color: red;
            color: white;
        }

        .logout-btn:hover {
            background-color: darkred;
        }

        .mypage-btn {
            background-color: #4CAF50;
            color: white;
        }

        .mypage-btn:hover {
            background-color: #388e3c;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>환영합니다! 🎉</h2>
        <p>로그인에 성공하셨습니다.</p>

        <a href="/mypage"><button class="btn mypage-btn">마이페이지</button></a>
        <button class="btn logout-btn" onclick="logout()">로그아웃</button>
    </div>

    <script>
        function logout() {
            fetch('/auth/logout', { method: 'POST' })
                .then(() => {
                    document.cookie = "token=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";
                    window.location.href = '/';
                })
                .catch(error => console.error('로그아웃 오류:', error));
        }
    </script>
</body>
</html>
